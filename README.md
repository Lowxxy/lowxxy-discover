Lowxxy Discover — Phone Beam Lens Fix

This version uses the last good Phone Photo + Light Fix as the base.

Only the PHONE projector light was changed:
- beam origin moved to the visible front camera lens
- beam angle lowered to match the lens direction
- flare moved to the same lens point
- desktop CSS is unchanged
- shirt photo size is unchanged
- hero/button layout is unchanged
- lookbook is unchanged

Replace the existing files in your Lowxxy Discover GitHub repo with the files in this folder.
